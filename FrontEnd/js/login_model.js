/* eslint-disable no-unused-vars */
class LoginModel {
  constructor() {
    this.email = "";
    this.password = "";
  }

  setEmail(email) {
    this.email = email;
  }

  setPassword(password) {
    this.password = password;
  }
}
